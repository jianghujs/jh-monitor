# 报告数据收集、分析、发送流程优化方案

## 背景

- 发送报告只能通过江湖面板发送，并且一个服务器一个服务器发送，容易被识别为垃圾邮件。
- 数据收集、数据分析、消息发送耦合在一起，容易受当时服务器运行状态、SSH 连通性和瞬时异常影响。
- 数据目前主要保存在面板本地 sqlite 中，没有统一沉淀到 ES 做集中管理、查询和追溯。

## 总体思路

将现有流程拆成三段：

1. 数据收集：远端主机定时生成原始数据，并统一通过filebeat收集到 ES。
2. 数据分析：扫描当天的原始数据，生成当天的日报，包括每个主机的报告和全部主机的概览报告。
3. 数据发送：从 ES 读取已经分析完成且校验通过的报告，再统一发送。

这样做的好处是：

- 采集、分析、发送解耦，链路更稳定。
- 报告以周期数据为准，不受发送当时机器状态影响。
- 原始数据和最终报告都能留痕，方便追溯、补算和重发。

## 一、新增配置功能

在 `route/templates/default/config.html` 增加“服务器报告配置”区域，统一管理报告链路所需配置。

### 配置项

- ES 相关配置，至少包含：
  - ES 地址
  - ES 用户名 / 密码（如启用鉴权）
- 服务器报告异常阈值统一配置，至少包含：
  - CPU 阈值
  - 内存阈值
  - 磁盘阈值
  - SSL 证书到期阈值

### 配置存储

- 配置存储方式参考现有 `data/notify.json`、`data/growth_alarm.json` 等 JSON 配置文件实现。
- 服务器报告异常阈值使用独立配置文件 `data/report_config.json` 保存。
- ES 相关配置使用单独配置文件 `data/es.json` 保存，与报告阈值配置分离管理。
- 配置文件不存在时自动初始化为 `{}`。
- 配置保存时优先写入 JSON；为了避免写入中断导致配置损坏，建议采用临时文件写入后再替换正式文件。
- 前端页面只负责配置录入与展示，实际保存、读取、校验由后端配置接口统一处理。

## 二、报告数据收集

### 目标

采集各主机报告所需的原始数据，统一写入 ES，作为后续分析的数据来源。

### 方案

- 需要收集两类数据：
  - 实时系统数据（如：CPU、内存、磁盘使用率等）
  - 备份日志类（如：xtrabackup备份记录、mysqldump 备份记录 等）数据。
- 实时运行数据可采用两种方式落 ES：
  - 方式 A：通过 ansible 定时远程执行采集脚本，脚本执行后直接写入 ES。
  - 方式 B ✨：在客户端起一个定时执行的采集进程，先写本地日志，再由 filebeat 采集并写入 ES。
- xtrabackup、xtrabackup 增量备份、backup 这 3 个数据直接由 filebeat 对接各自日志文件或 JSON 文件，再写入 ES。
- `scripts/client/report_collector.py` 除了继续输出明细类原始记录外，还需要参考 `/www/server/jh-panel/scripts/report.py` 的口径，把 xtrabackup、xtrabackup 增量备份、mysqldump 的最新状态快照直接写入 `host-system-status` 日志，减少后续日报分析时对分散数据源的重复读取。
- 原始数据只负责记录事实，不直接拼装最终报告。

### 安装与更新客户端定时采集任务

- 客户端定时采集任务的安装逻辑统一放到 `scripts/client/install/debian.sh`。
- 客户端采集采用“采集脚本 + cron 定时执行”的方式。
- 采集频率建议默认设置为每 5 分钟执行一次，与当前报告分析中“5 分钟内视为同一异常区间”的口径保持一致。
- 安装阶段需要完成采集脚本部署、cron 注册，并在安装完成后立即执行一次采集，确保安装完成后即可开始生成本地采集数据。
- 采集任务需要配置为开机后自动生效；采用 cron 时，通过系统 cron 配置保证机器重启后仍会按周期执行。
- 在客户端更新阶段，需要兼顾旧 cron 清理、脚本覆盖、权限修复、重新注册 cron，避免出现重复任务或残留旧脚本。
- 更新脚本需要具备幂等性：重复执行时不会重复注册同一条 cron，不会造成同一采集任务被重复触发。
- `scripts/client/install/debian.sh` 需要负责完成采集脚本安装、cron 写入、安装时立即执行一次采集脚本、更新后的 cron 重载与校验。
- 更新时若 cron 已存在，应按“清理旧 cron -> 覆盖程序/配置 -> 重新注册 cron -> 执行一次采集脚本 -> 校验输出结果”的顺序执行。

### cron 实现建议

- 建议使用独立 cron 文件，例如 `/etc/cron.d/jh-monitor-report-collector`，避免直接修改 root 用户现有 crontab，便于安装、更新和删除。
- 客户端采集脚本仍安装在 `ansible_user` 用户目录下，但定时任务改为由 `root` 用户执行，兼顾目录结构统一和运行权限充足。
- 建议 cron 规则默认采用 `*/5 * * * * root <采集命令>` 的形式执行。
- 采集命令需要显式指定解释器、脚本路径和日志输出路径，避免 cron 环境与交互式 shell 环境不一致导致执行失败。
- 安装时先清理旧版 cron 配置，再写入新版 cron 文件，最后校验配置内容和文件权限。
- 安装完成后应立即手动执行一次采集脚本，确保不必等待下一个 cron 周期才有首批数据。
- 更新时除重新写入 cron 外，还需要清理已废弃的旧脚本路径、旧日志路径或旧配置引用，避免 cron 继续调用旧版本文件。

### 建议路径

- 客户端采集脚本和输出文件仍放在 `ansible_user` 用户目录下，保持与 `scripts/client/install.sh` 现有目录约定一致。
- 采集脚本目录建议使用 `/home/${USERNAME}/jh-monitor-scripts/`。
- 采集脚本入口建议放在 `/home/${USERNAME}/jh-monitor-scripts/report_collector.py`。
- 原始 JSON 输出目录建议放在 `/home/${USERNAME}/jh-monitor-data/`。
- 采集任务运行日志建议写入 `/home/${USERNAME}/jh-monitor-logs/report-collector.log`。
- cron 配置文件建议使用 `/etc/cron.d/jh-monitor-report-collector`。
- 若需要锁文件避免 cron 重入，建议使用 `/tmp/jh-monitor-report-collector.lock`。

### `debian.sh` 安装 / 更新流程建议

- 安装阶段：
  - 准备 Python 运行环境与依赖；
  - 确认 root 运行环境、`ansible_user` 目录和依赖完整；
  - 创建 `/home/${USERNAME}/jh-monitor-scripts/`、`/home/${USERNAME}/jh-monitor-scripts/data/`、`/home/${USERNAME}/jh-monitor-logs/`；
  - 创建原始 JSON 输出目录和日志目录；
  - 部署或覆盖采集脚本到 `/home/${USERNAME}/jh-monitor-scripts/`；
  - 修正脚本、目录及日志文件权限，确保 root cron 执行时可读可写；
  - 写入 `/etc/cron.d/jh-monitor-report-collector`；
  - 立即执行一次采集脚本；
  - 校验输出目录中是否成功生成首批 JSON 文件。
- 更新阶段：
  - 删除旧版 cron 配置或清理旧规则；
  - 清理已废弃脚本、旧日志路径引用、旧输出目录引用；
  - 覆盖新版采集脚本和相关配置；
  - 重新写入 cron 配置；
  - 立即执行一次采集脚本；
  - 校验 cron 配置、脚本退出码和首批输出结果。
- 回滚与容错：
  - 若写入 cron 或首次执行失败，安装脚本应输出明确错误日志；
  - 若输出目录不存在、权限不足或依赖缺失，应直接终止安装并提示修复项；
  - 若更新失败，不应保留重复 cron 规则，避免同一台机器被重复采集。

### 用户权限配置建议

- 由于日报采集需要读取 `/www/server/jh-panel`、`/www/server/mysql-apt`、`/www/server/xtrabackup`、`/www/server/xtrabackup-inc`、`/www/server/keepalived` 等目录，如果完全放在 `ansible_user` 下执行，会引入较多 ACL、日志权限和 sudo 白名单维护成本。
- 第一版方案调整为：客户端日报采集脚本文件保留在 `ansible_user` 目录中，但 cron 任务由 `root` 用户执行，优先减少运行时权限适配复杂度。
- 这样做的好处是：
  - 不需要为日报采集额外开放大量运行时 ACL；
  - 不需要为日报采集额外扩充 `ansible_user` 的 sudo 白名单；
  - 仍能复用现有 `scripts/client/install.sh` 中的用户目录约定；
  - 安装、更新、故障排查路径更直接。
- `scripts/client/install.sh` 中现有的 `config_ansible_user` 仍保留，用于 SSH 连通、远程运维和其他非 root 侧操作，但不再作为日报采集脚本的运行身份前提。
- 若后续仍需要为 `ansible_user` 保留部分只读能力，建议仅保留现有远程运维所需的最小权限，例如：
  - `iptables -L`
  - `smartctl`
  - `sensors`
  - `ipmitool`
- 若将来再次尝试把采集脚本切回非 root 运行，再单独引入权限拆分脚本，不在第一版实施范围内。

### 采集数据模板

原始数据模板分成四个，对应ES中的四个索引：
  - host-system-status：实时运行数据
  - host-xtrabackup：xtrabackup 全量备份记录
  - host-xtrabackup-inc：xtrabackup 增量备份记录
  - host-backup：backup记录

#### 1. 实时运行数据模板

这份数据用于记录某台主机在某次采集时刻的完整运行状态，便于后续按主机、日期和模块进行汇总分析。

```json
{
  // 主机信息
  "host": {
    
    "host_id": "host-001",
    "host_name": "服务器A",
    "host_ip": "192.168.1.10",
    "host_group": "",
    "host_status": "running"
  },
  // 系统状态信息
  "system": {
    "cpu": 50,
    "memory": 50,
    "load": {
      "pro": 0.01,
      "one": 0.01,
      "five": 0.02,
      "fifteen": 0.03
    },
    "disks": [
      {
        "path": "/",
        "size": ['100GB', '50GB', '50GB', '50%'],
        "inodes": ['10000', '5000', '5000', '50%']
      }
    ]
  },
  // 网站状态列表
  "site": [
    { 
      "id": 32,
      "name": "jianhujs.org",
      "path": "/www/wwwroot/jianhujs.org",
      "ps": "jianhujs.org",
      "status": "运行中",
      "ssl_status": "未配置",
      "ssl_type": "lets",
      "ssl_data": {
        "issuer": "R3",
        "notAfter": "2026-04-04",
        "notBefore": "2026-04-04",
        "subject": "jianhujs.org",
        "organization": ["jianhujs.org"],
      },
      "add_time": "2025-07-11 06:59:00"
    }
  ],
  // JianghuJS 项目状态列表
  "jianghujs": [
    {
      "id": 1,
      "name": "1table-crud",
      "path": "/www/wwwroot/1table-crud",
      "status": "运行中",
      "add_time": "2025-07-11 06:59:00"
    }
  ],
  // Docker 项目状态列表
  "docker": [
    {
      "id": 1,
      "name": "docker-elk",
      "path": "/www/wwwroot/docker-elk",
      "status": "运行中",
      "add_time": "2025-07-11 06:59:00"
    }
  ],
  "mysql": {
    // 数据库总量，字符串字段用于展示，bytes 字段用于计算
    "total_size": "100MB",
    "total_size_bytes": "102400000",
    // 主从状态列表
    "slave_status": [
      {
        "ip": "127.0.0.1",
        "user": "root",
        "log_file": "mysql-bin.000001",
        "io_running": "Yes",
        "sql_running": "Yes",
        "delay": "0",
        "error_msg": "",
        "ps": "mysql-test",
        "add_time": "2026-04-04 00:10:00"
      }
    ],
    // 数据库表或库的容量明细
    "tables": [
      {
        "id": 1,
        "pid": 1,
        "table_name": "mysql-test",
        "size": "10MB",
        "size_bytes": "10240000",
      }
    ]
  },
  "backup": {
    "xtrabackup": {
      "enabled": true,
      "last_backup_time": "2026-04-04 03:00:00",
      "last_backup_timestamp": 1723172400,
      "last_backup_size": "2.04 GB",
      "last_backup_size_bytes": 2190067266,
      "count_in_timeframe": 1,
      "status": "normal"
    },
    "xtrabackup_inc": {
      "enabled": true,
      "full": {
        "last_backup_time": "2026-04-04 01:00:00",
        "last_backup_timestamp": 1723165200,
        "last_backup_size": "1.98 GB",
        "last_backup_size_bytes": 2126008811,
        "count_in_timeframe": 1,
        "status": "normal"
      },
      "inc": {
        "last_backup_time": "2026-04-04 12:00:00",
        "last_backup_timestamp": 1723204800,
        "last_backup_size": "118.04 MB",
        "last_backup_size_bytes": 123775595,
        "count_in_timeframe": 4,
        "status": "normal"
      }
    },
    "mysql_dump": {
      "enabled": true,
      "last_backup_time": "2026-04-04 01:30:00",
      "last_backup_timestamp": 1723167000,
      "last_backup_filename": "app_20260404_013000.tar.gz",
      "last_backup_path": "/www/backup/database/app_20260404_013000.tar.gz",
      "last_backup_size": "38.68 MB",
      "last_backup_size_bytes": 40561561,
      "count_in_timeframe": 3,
      "abnormal_files_in_timeframe": 0,
      "status": "normal"
    }
  },
  "lsync": {
    "last_realtime_sync_date": "2026-04-04",
    "last_realtime_sync_timestamp": 1724675696.5904987,
    "realtime_delays": 0,
    "send_count": 2,
    "send_open_count": 2,
    "send_close_count": 0
  },
  "rsync": [
    // 分析定时同步最新的log文件，提取最后一个日志文件，并检查日志内容是不是有total size，有的话说明是正常的同步
    { "name": "wwwstorage@x.x.x.x", "last_sync_date": "2026-04-04", "last_sync_timestamp": 1724675696.5904987, "last_sync_status": "成功", "last_sync_error_msg": "" }
  ]
  "add_time": "2026-04-04 00:10:00"
}
```

补充说明：

- `host-system-status` 里的 `backup` 字段用于记录“当前采集时看到的最新备份状态快照”。
- `host-xtrabackup`、`host-xtrabackup-inc`、`host-backup` 这三个索引仍保留明细记录，用于追溯和复算。
- 日报分析优先读取 `host-system-status.backup` 中的最新状态；如需进一步核对明细，再回查对应明细索引。

#### 2. xtrabackup 备份记录模板

来源文件：`/www/server/xtrabackup/data/backup_history.json`

每条记录表示一次 xtrabackup 全量备份结果，重点保留主机标识、备份时间和备份大小，供日报中的备份状态分析使用。

```json
{
    "host_id": "host-001",
    "host_name": "服务器A",
    "host_ip": "192.168.1.10",
    "add_time": "2024-08-26 20:34:56",
    "add_timestamp": 1724675696.5904987,
    "id": "1724675696.4536622",
    "size": "2.04 GB",
    "size_bytes": 2190067266.0
}

```

#### 3. xtrabackup 增量备份记录模板

来源文件：`/www/server/xtrabackup-inc/data/backup_history.json`

每条记录表示一次增量备份结果，结构和全量备份保持一致，额外通过 `backup_type` 区分备份类型，便于统一分析。

```json
{
    "host_id": "host-001",
    "host_name": "服务器A",
    "host_ip": "192.168.1.10",
    "add_time": "2024-08-26 14:30:22",
    "add_timestamp": 1724653822.7295759,
    "backup_type": "inc",
    "id": "1724653822.8486972",
    "size": "118.04 MB",
    "size_bytes": 123775595.0
}

```

#### 4. backup 事件记录模板

来源文件：`/www/server/jh-panel/logs/backup.log`

这类数据用于记录 mysqldump、网站备份、插件数据备份等通用备份事件。建议后续统一按日志方式写入，便于 filebeat 直接采集和区分不同备份类型。

```json
{
  // 备份类型，如 mysqldump、site_backup、plugin_backup 等
  "type": "mysqldump",
  // 备份文件名
  "filename": "1table_crud_20260303_013001.tar.gz",
  "path": "/www/backup/mysqldump/1table_crud_20260303_013001.tar.gz",
  "size": "10MB",
  "size_bytes": "100000",
  "add_time": "2024-08-26 20:34:56"
}
```

## 三、报告数据分析

### 目标

将当天的原始数据汇总分析，生成当天的日报，并写入 ES 的报告索引。报告分为两类：每个主机的详细报告，以及全部主机的概览报告。

### 方案

- 在`江湖云监控`增加独立的分析任务，每天凌晨1点执行。
- 从 ES 中读取某台主机当天的原始数据。
- 按统一规则进行汇总、异常识别、摘要生成，异常规则第一版对齐 `/www/server/jh-panel/scripts/report.py`。
- 先生成每个主机的详细报告，再汇总生成全部主机的概览报告。
- 生成最终报告后分别写入 `host-report-single` 和 `host-report-overview` 两个索引，作为发送的唯一数据来源。

### 产出内容

- 单主机报告：展示某台主机当天的资源、备份、网站和服务状态。
- 主机概览报告：汇总全部主机当天的整体情况，重点展示异常主机、异常项分布和整体运行摘要，并在尾部拼接当天全部主机的单机报告列表。

### 异常判断规则

以 `/www/server/jh-panel/scripts/report.py` 为准，主要规则如下：

- 系统资源阈值沿用当前通知配置：`cpu`、`memory`、`disk`、`ssl_cert`。
- CPU、内存、资源使用率按统计周期内平均值判断；监控点超过阈值时，按“5 分钟内只计 1 次异常”的方式统计 `overCount`。
- 磁盘使用率按各挂载点当前使用百分比判断，超过 `disk` 阈值即标记异常。
- 最后监控时间早于统计周期开始时间时，判定为“系统监控状态异常”。
- 网站证书异常沿用现有逻辑：非 `lets` / `acme` 证书在剩余有效期小于 `ssl_cert` 配置时记为异常。
- MySQL 主从状态异常沿用现有逻辑：`io_running != Yes`、状态记录不是当天产生、`delay` 为空或大于 0，均判定为异常。
- Xtrabackup 全量备份当天没有成功记录时，判定为异常。
- Xtrabackup 增量备份当天缺少全量或增量成功记录时，判定为异常。
- MySQL Dump 当天没有备份记录，或最后一次备份时间早于统计周期开始时间时，判定为异常。
- Rsyncd/实时备份沿用现有逻辑：已启用的实时同步或定时同步在当天没有成功同步记录时，判定为异常；`realtime_delays > 0` 记为告警提示。
- Keepalived 服务状态不是 `start` 时，判定为异常。
- 若当天没有任何异常摘要，则默认生成“服务运行正常，继续保持”。

说明：

- 单主机报告仍会展示网站、JianghuJS、Docker、MySQL 容量等明细。
- 第一版“是否异常主机”的判断先完全复用 `report.py` 的现有口径，后续如需扩大到站点停机、容器停机等规则，再单独补充。
- 分析任务读取异常阈值时，统一从新的“服务器报告配置”中取值，不再分散写死在任务代码中。

### 单主机报告内容模板

参考现有单机报告数据和页面结构，建议单主机报告继续保留以下结构：

```json
{
  // 报告类型，单主机固定为 single_host
  "report_type": "single_host",
  "report_time": "2026-04-05 01:00:00",
  // 统计时间范围
  "start_time": "2026-04-04 00:00:00",
  "end_time": "2026-04-04 23:59:59",
  "start_date": "2026-04-04",
  "end_date": "2026-04-04",
  // 主机展示信息
  "host_id": "host-001",
  "host_name": "服务器A",
  "host_ip": "192.168.1.10",
  // 可直接发送和预览的 HTML 内容
  "html_content": "<html>...</html>",
  // 概要信息列表，自动生成当天最重要的异常和结论
  "summary_tips": [],
  // 异常标签列表，可用于判断是否属于异常主机
  "error_tips": [],
  // 系统类展示内容
  "sysinfo_tips": [
    { "name": "CPU", "desc": "平均使用率：50%\n当前使用率：5%" },
    { "name": "内存", "desc": "平均使用率：30%\n当前使用率：25%" },
    { "name": "资源使用率", "desc": "平均使用率：40%" },
    { "name": "磁盘（/）", "desc": "已使用 80%（80G/100G）" },
    { "name": "最后监控时间", "desc": "2026-04-04 23:59:00" }
  ],
  // 结构化备份分析结果，便于后续查询和复算
  "mysql_master_slave_info": {
    "is_slave": true
  },
  "xtrabackup_info": {
    "last_success_time": "2026-04-04 03:00:00",
    "size": "2.04 GB",
    "size_bytes": 2190067266
  },
  "xtrabackup_inc_info": {
    "last_success_time": "2026-04-04 12:00:00",
    "size": "118.04 MB",
    "size_bytes": 123775595
  },
  "mysql_dump_info": {
    "last_success_time": "2026-04-04 01:30:00",
    "filename": "app_20260404_013000.tar.gz"
  },
  "rsyncd_info": {
    "last_realtime_sync_date": null,
    "last_realtime_sync_timestamp": null,
    "realtime_delays": 0,
    "send_count": 2,
    "send_open_count": 2,
    "send_close_count": 0
  },
  // 备份类展示内容
  "backup_tips": [
    { "name": "主从同步", "desc": "IO：正常；SQL：正常；延迟：0 秒" },
    { "name": "xtrabackup", "desc": "最后成功时间：2026-04-04 03:00:00" },
    { "name": "增量备份", "desc": "最后成功时间：2026-04-04 12:00:00" },
    { "name": "MySQL Dump", "desc": "最后成功时间：2026-04-04 01:30:00" },
    { "name": "Rsyncd", "desc": "最后一次定时同步：2026-04-04 03:00:00" }
  ],
  // 业务类展示内容
  "siteinfo_tips": [
    { "name": "jianhujs.org", "desc": "运行中（SSL 正常）" }
  ],
  "jianghujsinfo_tips": [
    { "name": "1table-crud", "desc": "运行中" }
  ],
  "dockerinfo_tips": [
    { "name": "docker-elk", "desc": "运行中" }
  ],
  "mysqlinfo_tips": [
    { "name": "数据库总大小", "desc": "100MB" },
    { "name": "mysql-test", "desc": "10MB" }
  ],
  // 可选：保留渲染前的附加分析数据，便于后续扩展
  "extra_info": {}
}
```

### 全部主机概览报告内容模板

概览报告建议突出“整体情况 + 异常分布 + 重点主机”，结构可以比单主机更偏汇总：

```json
{
  "report_type": "overview",
  "title": "Mega08-全部主机概览报告",
  "report_time": "2026-04-05 01:00:00",
  "start_time": "2026-04-04 00:00:00",
  "end_time": "2026-04-04 23:59:59",
  "report_date": "2026-04-04",
  "html_content": "<html>...</html>",
  // 主机统计，概览报告顶部直接展示
  "host_overview_info": {
    "host_total": 10,
    "host_online": 9,
    "host_offline": 1,
    "host_error": 1
  },
  // 主机概况，主要描述正常主机和异常主机
  "host_overview_tips": [
    { "name": "正常主机", "desc": "192.168.1.21（服务器E）、192.168.1.22（服务器F）、192.168.1.23（服务器G）等 7 台" },
    { "name": "异常主机", "desc": "192.168.1.10（服务器A）共 1 台，需要重点关注" }
  ],
  // 异常主机摘要，优先展示每台异常主机对应的单机 summary_tips 汇总结果
  "exception_host_summary_tips": [
    { "name": "192.168.1.10（服务器A）", "desc": "磁盘（/）平均使用率过高，有服务中断停机风险" }
  ],
  "single_host_report_list": [
    {
      "host_id": "host-001",
      "host_name": "服务器A",
      "host_ip": "192.168.1.10",
      "is_error": true,
      "summary_tips": ["磁盘（/）平均使用率过高，有服务中断停机风险"],
      "html_content": "<html>单机报告片段...</html>"
    }
  ],
  // 可选：保留一些结构化汇总字段，便于后续查询、筛选和扩展
  "extra_info": {
    "normal_host_count": 7,
    "offline_host_count": 1,
    "error_host_ids": ["host-001"]
  }
}
```

## 四、报告数据发送

### 目标

从 ES 中读取当天已经生成且有效的报告，再统一发送，避免发送时临时去主机上取数和计算。

发送范围控制为：只发送异常服务器的单主机报告，以及全部主机的概览报告。

### 方案

- 在江湖云监控增加独立的发送任务，按报告配置定时执行。
- 发送渠道固定为邮件，沿用现有通知配置和 `jh.notifyMessage` 邮件发送能力。
- 发送时只从 `host-report-single` 和 `host-report-overview` 读取已经分析完成且校验通过的报告。
- 单主机报告只发送当天识别为异常的服务器。
- 每天固定发送一份全部主机的概览报告，且该报告尾部附带当天全部主机的单机报告列表。
- 报告内容中保存 HTML，发送时优先直接使用已生成的 HTML 内容。
- 对发送前的数据做时间和完整性检查。
- 发送成功后记录发送状态，失败时记录失败原因并支持重试。
- 发送任务、分析任务、采集任务访问 ES 时，统一读取同一套 ES 配置，避免多处维护。

### 发送使用的 HTML 模板

#### 单主机报告发送效果示例

```text
服务器A（192.168.1.10）-服务器运行报告
日期：2026-04-04 至 2026-04-04（报告时间：2026-04-05 01:00:00）

概要信息：
- 磁盘（/）平均使用率过高，有服务中断停机风险
- docker-elk 已停止，需要尽快检查容器状态

系统状态：
+----------------+--------------------------------------------+
| CPU            | 平均使用率：50% / 当前使用率：5%            |
| 内存           | 平均使用率：30% / 当前使用率：25%           |
| 资源使用率     | 平均使用率：40%                             |
| 磁盘（/）      | 已使用 80%（80G/100G）                     |
| 最后监控时间   | 2026-04-04 23:59:00                        |
+----------------+--------------------------------------------+

备份：
+----------------+--------------------------------------------+
| 主从同步       | IO：正常；SQL：正常；延迟：0 秒            |
| xtrabackup     | 最后成功时间：2026-04-04 03:00:00          |
| 增量备份       | 最后成功时间：2026-04-04 12:00:00          |
| MySQL Dump     | 最后成功时间：2026-04-04 01:30:00          |
| Rsyncd         | 最后一次定时同步：2026-04-04 03:00:00      |
+----------------+--------------------------------------------+

网站：
+----------------+--------------------------------------------+
| jianhujs.org   | 运行中（SSL 正常）                         |
+----------------+--------------------------------------------+

JianghuJS项目：
+----------------+--------------------------------------------+
| 1table-crud    | 运行中                                     |
+----------------+--------------------------------------------+

Docker项目：
+----------------+--------------------------------------------+
| docker-elk     | 已停止                                     |
+----------------+--------------------------------------------+

数据库：
+----------------+--------------------------------------------+
| 数据库总大小   | 100MB                                      |
| mysql-test     | 10MB                                       |
+----------------+--------------------------------------------+
```

#### 全部主机概览报告发送效果示例

```text
Mega08-全部主机概览报告（2026-04-04）
日期：2026-04-04 00:00:00 至 2026-04-04 23:59:59（报告时间：2026-04-05 01:00:00）

主机统计：
+----------------+--------------------------------------------+
| 主机总数       | 10                                         |
| 在线主机       | 9                                          |
| 离线主机       | 1                                          |
| 异常主机       | 1                                          |
+----------------+--------------------------------------------+

主机概况：
+----------------+--------------------------------------------+
| 正常主机       | 192.168.1.21（服务器E）、192.168.1.22（服务器F）、192.168.1.23（服务器G）等 7 台 |
| 异常主机       | 192.168.1.10（服务器A）共 1 台，需要重点关注 |
+----------------+--------------------------------------------+

异常主机摘要：
+----------------------+------------------------------------------+
| 192.168.1.10（服务器A） | 磁盘（/）平均使用率过高，有服务中断停机风险 |
+----------------------+------------------------------------------+

主机单机报告列表：

[1] 服务器A（192.168.1.10）
- 概要信息：磁盘（/）平均使用率过高，有服务中断停机风险
- 详情：附完整单机报告 HTML

[2] 服务器B（192.168.1.11）
- 概要信息：服务运行正常，继续保持！
- 详情：附完整单机报告 HTML
```

- 增加周报：
  - 一周服务器概览
  - 近一周的变化曲线

## 五、实施任务清单

### P0：先打通主链路

- [ ] 在 `route/templates/default/config.html` 增加“服务器报告配置”入口，支持阈值配置和 ES 配置的录入与展示。
- [ ] 增加配置读写接口与持久化存储，将服务器报告阈值保存到 `data/report_config.json`，将 ES 连接参数保存到 `data/es.json`。
- [ ] 建立并验证 ES 索引模板：`host-system-status`、`host-xtrabackup`、`host-xtrabackup-inc`、`host-backup`、`host-report-single`、`host-report-overview`。
- [ ] 实现客户端采集脚本，按统一 JSON 结构输出实时系统数据到本地文件，默认按 5 分钟周期执行。
- [ ] 约定客户端 `ansible_user` 目录下的采集脚本、输出目录、运行日志路径，以及 `root` 用户下的 cron 配置路径，确保 filebeat 与安装脚本使用同一套路径。
- [ ] 调整 `scripts/client/install/debian.sh` 的安装逻辑，使日报采集脚本安装在用户目录下，但由 `root` cron 执行。
- [ ] 评估并保留 `scripts/client/install.sh` 中 `config_ansible_user` 的最小必要能力，仅服务于 SSH 连通和远程运维，不再承担日报采集授权职责。
- [ ] 将客户端定时采集任务的安装集成到 `scripts/client/install/debian.sh`，安装时自动写入独立 cron 配置并立即执行一次采集脚本。
- [ ] 补齐客户端更新逻辑：清理旧 cron、覆盖新脚本、重新注册 cron，并确保重复执行时不会产生重复任务或脏文件。
- [ ] 配置 filebeat，完成实时系统数据、本地备份 JSON、`backup.log` 到 ES 的采集入库。
- [ ] 统一原始数据字段：`host_id`、`host_name`、`host_ip`、`add_time`、`add_timestamp`、状态字段和字节数字段。
- [ ] 在江湖云监控 task.py 中新增“每日报告分析任务”，按天聚合 ES 原始数据。
- [ ] 按 `report.py` 现有规则实现单主机报告分析，产出 `summary_tips`、`error_tips`、结构化明细和 `html_content`。
- [ ] 生成全部主机概览报告，并在尾部拼接当天全部主机的单机报告列表。
- [ ] 将单主机报告写入 `host-report-single`，将概览报告写入 `host-report-overview`。
- [ ] 在江湖云监控 task.py 中新增“每日统一报告发送任务”，按邮件方式发送“异常单主机报告 + 全部主机概览报告”。

### P1：对齐现有规则和发送细节

- [ ] 将异常判断与 `/www/server/jh-panel/scripts/report.py` 完整对齐，包含 CPU、内存、负载、磁盘、SSL、主从、xtrabackup、增量备份、mysqldump、Rsyncd、Keepalived。
- [ ] 将阈值读取方式与现有通知配置对齐，避免新旧日报口径不一致。
- [ ] 为 ES 配置增加连通性校验能力，至少支持保存前测试连接。
- [ ] 为阈值配置增加默认值、范围校验和空值兜底。
- [ ] 复用 `/www/server/jh-monitor/task.py` 中 `hostReportNotifyTask` 的邮件配置检查和发送能力。
- [ ] 增加发送前校验：报告日期、统计区间、HTML 是否为空、单机/概览是否生成完整。
- [ ] 增加发送状态记录：发送时间、发送对象、成功/失败状态、失败原因、重试次数。
- [ ] 增加分析结果校验：按主机、按日期确认原始数据完整，避免因漏采导致误报。

### P2：补齐运维能力和增强功能

- [ ] 增加报告重算能力，支持按日期重跑分析任务。
- [ ] 增加邮件补发能力，支持按单主机报告或概览报告重发。
- [ ] 增加周报能力，输出一周服务器概览。
- [ ] 增加近一周变化曲线，用于展示资源和异常趋势。
- [ ] 评估是否补充站点停机、JianghuJS 项目停机、Docker 容器停机等异常规则，并作为后续扩展项单独推进。
