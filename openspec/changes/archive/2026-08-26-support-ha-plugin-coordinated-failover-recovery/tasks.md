## 1. 插件协调状态与对端检查

- [x] 1.1 梳理 `/www/server/jh-panel/plugins/ha_manager/index.py` 现有 SSH 绑定、对端调用、切换锁、自检和切换入口。
- [x] 1.2 新增或复用插件间协调状态读取方法，返回当前角色、降级状态、待切换主机、待切换目标角色、故障切换时间和恢复要求。
- [x] 1.3 新增对端执行能力检查方法，验证 SSH 可达、对端插件存在、插件入口可执行、对端配置完整和切换锁状态。
- [x] 1.4 将对端检查结果写入 `/www/server/logs` 下的主备管理插件交互日志。

## 2. 切换执行模式判断

- [x] 2.1 在插件切换入口前增加执行模式判断，输出 `full_switch`、`local_failover` 或 `blocked_remote_unreachable`。
- [x] 2.2 当本机和对端均可执行时，允许选择任一主机并记录协调主机 `coordinator_host_id`。
- [x] 2.3 当对端不可达且目标为本机时，提示将跳过对端下线并进入降级运行。
- [x] 2.4 当对端不可达且目标为对端时，拒绝执行并返回明确错误原因。

## 3. 完整双边切换协调

- [x] 3.1 在 `full_switch` 且目标为本机时，复用现有流程执行本机上线和远程对端下线。
- [x] 3.2 在 `full_switch` 且目标为对端时，复用现有流程远程触发对端上线并执行本机下线。
- [x] 3.3 确保双边切换过程中的执行方式、目标主机、阶段和失败原因上报到云监控。
- [x] 3.4 确保双边切换仍受现有 `switch.lock` 和云监控任务领取状态保护，避免重复执行。

## 4. 本机故障升主与降级状态

- [x] 4.1 为 `local_failover` 增加本机故障升主流程，跳过不可达对端下线阶段。
- [x] 4.2 在故障升主前要求操作员确认对端已停机、隔离或不会继续写入。
- [x] 4.3 故障升主成功后持久化 `failover_state.json` 或等价状态，包含当前主、待切换主机、待切换目标角色、不可达主机、协调主机、故障原因和恢复要求。
- [x] 4.4 故障升主失败时保留失败状态和日志，不把主备关系标记为正常。
- [x] 4.5 故障升主后上报云监控 `degraded_master`、`pending_switch_required`、`pending_switch_host_id`、`pending_switch_role` 和降级原因。

## 5. task 触发旧主恢复保护与恢复为备机

- [x] 5.1 在插件 `info.json` 或等价任务声明中增加恢复检查定时任务，确保 `/www/server/jh-panel/task.py` 会周期触发。
- [x] 5.2 在插件恢复检查入口中实现角色不匹配判断，确认本机是否匹配 `pending_switch_host_id`、`pending_switch_role=standby` 且仍处于主角色或主角色配置未清理。
- [x] 5.3 旧主恢复后连接当前新主插件，读取对端协调状态并记录判断依据。
- [x] 5.4 匹配待切换标识后发送邮件通知，通知当前主机、待切换主机、目标角色、恢复方式和操作入口。
- [x] 5.5 自动恢复未开启时进入 `recovery_guard`，阻止本机按正常主身份完成主备闭环。
- [x] 5.6 插件 UI 增加“待切换为备机”提示、人工确认恢复入口和自动恢复配置。
- [x] 5.7 自动恢复开启时，由恢复检查入口自动领取并执行 `recover_as_standby`。
- [x] 5.8 为 `recover_as_standby` 增加本地恢复锁和执行状态记录，避免 task 进程重复触发。
- [x] 5.9 实现 `recover_as_standby` 流程，复用备机化脚本、配置恢复、同步重建、自检和状态上报能力。
- [x] 5.10 恢复成功后清除双方插件的待切换状态，并确保后续上报显示一主一备正常。

## 6. 云监控展示与日志归档

- [x] 6.1 调整 `/www/server/jh-monitor/class/core/ha_api.py`，兼容保存插件上报的降级运行、待切换标识、恢复保护、恢复中和恢复完成字段。
- [x] 6.2 调整云监控主备列表和详情页，展示降级运行、当前主机、待切换主机、目标角色、降级原因和恢复状态。
- [x] 6.3 调整云监控切换弹框，展示插件判断的执行方式和不可达时的操作限制。
- [x] 6.4 调整云监控日志展示，区分完整双边切换、本机故障升主、恢复保护和恢复为备机。
- [x] 6.5 确保云监控不下发旧主恢复为备机任务，只展示插件侧恢复状态和日志。

## 7. 验证

- [ ] 7.1 验证本机和对端都可达时，可以从本机房自由切换到任一主机。
- [x] 7.2 验证对端不可达且目标为本机时，可以故障升主并进入降级运行。
- [x] 7.3 验证对端不可达且目标为对端时，系统拒绝切换并提示原因。
- [x] 7.4 验证 task 进程周期触发恢复检查，但角色判断和恢复执行只在插件内完成。
- [ ] 7.5 验证旧主恢复后进入 `recovery_guard`，不会自动按旧主身份恢复正常。
- [ ] 7.6 验证人工确认恢复为备机后，旧主变为备机且双方上报恢复正常。
- [ ] 7.7 验证自动恢复配置关闭时不会静默执行恢复；开启后由 task 触发的插件检查自动执行恢复。
- [x] 7.8 运行 `python3 -m py_compile /www/server/jh-monitor/class/core/ha_api.py /www/server/jh-panel/plugins/ha_manager/index.py`。
- [x] 7.9 运行前端 JS 语法检查：`node --check /www/server/jh-monitor/route/static/app/ha_management.js`。
