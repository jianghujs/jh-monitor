## 1. 后端数据规范化

- [ ] 1.1 梳理 `ha_manager` 插件当前 `health_detail`、`script_checks`、切换字段和日志事件上报结构。
- [ ] 1.2 在 `class/core/ha_api.py` 中补充主机展示规范化逻辑，输出 `script_checks`、采集来源、切换字段、最近上报时间和兼容默认值。
- [ ] 1.3 在 `class/core/ha_api.py` 中补充主备关系展示规范化逻辑，输出当前 run 摘要、实际主机、期望主机、状态说明、日志路径和 warnings。
- [ ] 1.4 调整详情或日志接口，支持返回当前切换任务事件列表，并保留完整日志读取能力。

## 2. 前端真实展示

- [ ] 2.1 移除或停用 `ha_management.js` 中基于 `haCheckTemplate` 生成模拟状态的逻辑。
- [ ] 2.2 实现真实自检明细渲染，支持按分组展示 `script_checks`，缺失时展示摘要或暂无明细。
- [ ] 2.3 调整列表和主机行展示，使用后端返回的真实角色、在线状态、采集状态、健康状态和最近上报时间。
- [ ] 2.4 调整切换状态页，使用真实 run、host switch 字段和错误信息展示阶段、步骤、下一步和日志路径。
- [ ] 2.5 调整切换日志页，优先按 `origin_host_id` 分组展示事件日志，并保留完整日志视图。

## 3. 状态与兼容处理

- [ ] 3.1 确保状态等级由后端真实数据推导，前端只做样式和文本映射。
- [ ] 3.2 为老版本插件缺少 `script_checks`、缺少事件行、缺少日志文件等情况补充未知/暂无展示。
- [ ] 3.3 确保列表接口不无条件返回大日志内容，详情或日志弹窗按需读取。

## 4. 验证

- [ ] 4.1 新增或更新 HA API 测试，覆盖真实 `health_detail.script_checks` 的规范化输出。
- [ ] 4.2 新增或更新 HA UI API flow 测试，覆盖列表、详情、自检、切换状态和日志字段。
- [ ] 4.3 运行 `python3 -m py_compile /www/server/jh-monitor/class/core/ha_api.py`。
- [ ] 4.4 运行现有 HA 相关测试脚本并记录结果。
