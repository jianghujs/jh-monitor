###################### Filebeat Configuration Example #########################

# This file is an example configuration file highlighting only the most common
# options. The filebeat.reference.yml file from the same directory contains all the
# supported options with more comments. You can use it as a reference.
#
# You can find the full configuration reference here:
# https://www.elastic.co/guide/en/beats/filebeat/index.html

# For more available modules and options, please see the filebeat.reference.yml sample
# configuration file.

name: "<hostId>"

# ============================== Filebeat inputs ===============================
# All inputs are loaded from /etc/filebeat/inputs.d/*.yml, including:
#   - host-debian.yml  (host log inputs)
#   - jh-monitor-task-*.yml  (monitor task inputs)
# This directory is shared by the host installer and the monitor task installer.

filebeat.config.inputs:
  enabled: true
  path: /etc/filebeat/inputs.d/*.yml
  reload.enabled: true
  reload.period: 10s

# ============================== Filebeat modules ==============================

filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false
  reload.period: 10s

# ======================= Elasticsearch template setting =======================

setup.template.settings:
  index.number_of_shards: 1
  index.codec: best_compression
  index.refresh_interval: "5s"

setup.ilm.enabled: true
setup.ilm.check_exists: true

setup.template.name: "host-debian-<hostIdIndex>-logs"
setup.template.pattern: "host-debian-<hostIdIndex>-*"
setup.template.enabled: true
setup.template.overwrite: true
setup.template.priority: 200

# =================================== Kibana ===================================

# Starting with Beats version 6.0.0, the dashboards are loaded via the Kibana API.
# This requires a Kibana endpoint configuration.
setup.kibana:

  # Kibana Host
  host: "<serverIp>:5601"
  username: "elastic"
  password: "changeme"

# =============================== Elastic Cloud ================================

#cloud.id:
#cloud.auth:

# ================================== Outputs ===================================

# ---------------------------- Elasticsearch Output ----------------------------
output.elasticsearch:
  hosts: ["<serverIp>:9200"]
  indices:
    - index: "host-debian-<hostIdIndex>-system-status-%{+yyyy.MM}"
      when.equals:
        log_index: "host-debian-system-status"
    - index: "host-debian-<hostIdIndex>-xtrabackup-%{+yyyy.MM}"
      when.equals:
        log_index: "host-debian-xtrabackup"
    - index: "host-debian-<hostIdIndex>-xtrabackup-inc-%{+yyyy.MM}"
      when.equals:
        log_index: "host-debian-xtrabackup-inc"
    - index: "host-debian-<hostIdIndex>-backup-%{+yyyy.MM}"
      when.equals:
        log_index: "host-debian-backup"
    - index: "host-debian-<hostIdIndex>-common-%{+yyyy.MM}"
      when.equals:
        fields.log_type: "common"
    - index: "host-debian-<hostIdIndex>-syslog-%{+yyyy.MM}"
      when.equals:
        log_type: "syslog"
    - index: "host-debian-<hostIdIndex>-nginx-error-%{+yyyy.MM}"
      when.equals:
        nginx_log_kind: "error"
    - index: "host-debian-<hostIdIndex>-nginx-%{+yyyy.MM}"
      when.equals:
        log_type: "nginx"
    - index: "host-debian-<hostIdIndex>-eggjs-%{+yyyy.MM}"
      when.equals:
        log_type: "eggjs"
    - index: "host-debian-<hostIdIndex>-mysql-%{+yyyy.MM}"
      when.equals:
        log_type: "mysql"
    - index: "host-debian-<hostIdIndex>-jh-panel-%{+yyyy.MM}"
      when.equals:
        log_type: "jh-panel"
  pipelines:
    - pipeline: "nginx-pipeline"
      when.equals:
        nginx_log_kind: "access"
  preset: balanced

  username: "elastic"
  password: "changeme"

# ------------------------------ Logstash Output -------------------------------
#output.logstash:
  #hosts: ["localhost:5044"]

# ================================= Processors =================================

processors:
  - add_fields:
      target: ''
      fields:
        host_id: "<hostId>"
  - add_host_metadata:
      when.not.contains.tags: forwarded
  - add_cloud_metadata: ~
  - add_docker_metadata: ~
  - add_kubernetes_metadata: ~

# ================================== Logging ===================================

logging.level: info
logging.selectors: ["input"]
logging.metrics.enabled: false
